let initialState = {
    FriendData: [
    {id: 1, name: 'Masha'},
    {id: 2, name: 'Tanya'},
    {id: 3, name: 'Sasha'},
],
}

const navbarReducer = (state = initialState, action) => {
    return state;
}

export default navbarReducer;